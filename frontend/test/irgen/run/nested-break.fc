extern int printf(char[] format, ...);

int main() {
    int i = 0;

    do {
        i+=1;
        for (int i = 0 to 3) {
            break; //inner loop should stop outer shouldnt!
            printf("bad bad print\n");
        }
        printf("%d\n", i);
    } while(i < 5);

    printf("done\n");
    return 0;
}
